server:
  port: 8085
  servlet:
    context-path: /chat
spring:
  application:
    name: chat-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/chat-service?authSource=admin
      auto-index-creation: true # cho phép Spring tự động tạo index cho entity khi dùng MongoDB
app:
  services:
    profile:
      url: http://localhost:8081/profile
